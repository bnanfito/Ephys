% getMet
% written by Brandon Nanfito

% INPUTS:
% in = nRepetitions x nConditions matrix of response magnitude (firing rate; hz) for each trial
% domval = 

function [out] = getOriTuning(in,domval)

    

end